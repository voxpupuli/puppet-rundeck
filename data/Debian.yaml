---
rundeck::override_dir: /etc/default

rundeck::repo_config:
  rundeck:
    location: https://packages.rundeck.com/pagerduty/rundeck/any
    release: any
    repos: main
    key:
      name: rundeck.gpg
      source: https://docs.rundeck.com/keys/BUILD-GPG-KEY-20230105.key
